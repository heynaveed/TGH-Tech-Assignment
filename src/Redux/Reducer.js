const initialState={
    isLoding:false,
    isError:false,
    Bookmark:[]
}

const Reducer=(State=initialState,action)=>{
    const {type,payload}=action;
    switch(type){
        default:
            return State
    }
}

export{Reducer}